package tech.mogami.spring.autoconfigure.provider.facilitator;

import lombok.experimental.UtilityClass;

/**
 * Facilitator URLs.
 */
@UtilityClass
@SuppressWarnings("checkstyle:HideUtilityClassConstructor")
class FacilitatorURLs {

    /** Supported URL. */
    static final String SUPPORTED_URL = "/supported";

    /** Verify URL. */
    static final String VERIFY_URL = "/verify";

    /** Accept URL. */
    static final String ACCEPT_URL = "/accept";

}
